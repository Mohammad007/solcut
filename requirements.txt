# SlotCut backend — Python 3.11+ (tested on 3.13)
Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.7.1
Flask-SocketIO==5.4.1
Flask-Cors==5.0.0
Flask-Limiter==3.8.0
python-dotenv==1.0.1
marshmallow==3.23.1
Pillow==11.0.0
requests==2.32.3

# Real-time transport for SocketIO threading mode
simple-websocket==1.1.0

# External integrations (used only when configured; mocked in dev)
razorpay==1.4.2
firebase-admin==6.6.0

# Production server (Railway / gunicorn + eventlet worker for WebSockets).
# Locally the app uses async_mode=threading; in prod set SOCKETIO_ASYNC_MODE=eventlet.
gunicorn==23.0.0
eventlet==0.39.1
